// config.js

module.exports = {
    SMTP_USER: process.env.SMTP_USER || "rocketmediaportfolio@gmail.com",  // Coloca un valor por defecto si no está en las variables de entorno
    SMTP_PASS: process.env.SMTP_PASS || "yecz emdz kcwu busv"  // Coloca un valor por defecto si no está en las variables de entorno
  };
  